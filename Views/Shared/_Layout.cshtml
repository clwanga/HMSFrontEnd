<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />

    <title>@ViewBag.Title | Civil Registration And Vital Statistic</title>
    <partial name="_Cssheader" />
</head>
<body>
    <div class="min-h-full">
        <!-- Off-canvas menu for mobile, show/hide based on off-canvas menu state. -->
        <div id="sidebar"
             class="fixed inset-0 z-40 flex hidden"
             role="dialog"
             aria-modal="true">
            <div class="fixed inset-0 bg-gray-600 bg-opacity-75"
                 aria-hidden="true"></div>

            <div class="relative flex w-full max-w-xs flex-1 flex-col bg-crvsblue-700 pt-5 pb-4">
                <div class="absolute top-0 right-0 -mr-12 pt-2">
                    <button id="close_sidebar"
                            type="button"
                            class="ml-1 flex h-10 w-10 items-center justify-center rounded-full focus:outline-none focus:ring-2 focus:ring-inset focus:ring-white">
                        <span class="sr-only">Close sidebar</span>
                        <!-- Heroicon name: outline/x -->
                        <svg class="h-6 w-6 text-white"
                             xmlns="http://www.w3.org/2000/svg"
                             fill="none"
                             viewBox="0 0 24 24"
                             stroke="currentColor"
                             aria-hidden="true">
                            <path stroke-linecap="round"
                                  stroke-linejoin="round"
                                  stroke-width="2"
                                  d="M6 18L18 6M6 6l12 12" />
                        </svg>
                    </button>
                </div>
                <!--nav bar place-->
                <div>
                    <partial name="_navigation" />
                </div>
            </div>
        </div>

        <!-- Static sidebar for desktop -->
        <div class="hidden lg:fixed lg:inset-y-0 lg:flex lg:w-64 lg:flex-col">
            <!-- Sidebar component, swap this element with another sidebar if you like -->

            <div class="flex flex-grow flex-col overflow-y-auto bg-crvsblue-700 pt-5 pb-4">

                <!-- navigation section -->
                <div>
                    <partial name="_navigation" />
                </div>
            </div>
        </div>

        <div class="flex flex-1 flex-col lg:pl-64">
            <main id="mainpage" class="relative min-h-screen flex-1 pb-24">
                <!-- Header -->
                <div>
                    <partial name="_header" />
                </div>
                <!-- Main Content -->
                @RenderBody()

                <div>
                    <partial name="_footer" />
                </div>
            </main>
        </div>
    </div>
    <partial name="_FooterScripts" />
</body>
</html>
